1. Configure a global .gitignore - using `git config --global core.excludesfile ~/.gitignore`
2. `python3 -m venv .venv`
3. Activate the virtual env - `source .venv/bin/activate`
We can also use `cmd + shift + p` search Python: Select Interpreter, choose the one inside `venv`